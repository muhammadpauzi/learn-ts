describe("Data Type", function () {
    it("should must declare", function () {
        let name: string = "Muhammad Pauzi";
        let balance: number = 12000;
        let isVip: boolean = true;

        console.log(name);
        console.log(balance);
        console.log(isVip);

        // name = 120;
        // balance = "120";
        // isVip = "120";
    });
})